version: '2'
services:
  mon.ceph.wator.xyz:
    image: ceph/daemon
    network_mode: "host"
    volumes:
      - /ceph/conf/etc:/etc/ceph
      - ./ceph.conf:/etc/ceph/ceph.conf
      - /ceph/storage1/mon:/var/lib/ceph
    command: mon
    restart: 'no'
