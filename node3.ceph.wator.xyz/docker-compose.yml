version: '2'
services:
  mon.node3.ceph.wator.xyz:
    image: ceph/daemon
    network_mode: "host"
    volumes:
      - /ceph/conf/etc:/etc/ceph
      - /ceph/conf/ganesha:/etc/ganesha
      - /ceph/storage1/mon:/var/lib/ceph
    environment:
      - IP_LEVEL=6
      - MON_NAME=node3.ceph.wator.xyz
      - MON_IP=192.168.0.10
      - CEPH_PUBLIC_NETWORK=192.168.0.10/24
      - CEPH_CLUSTER_NETWORK=192.168.0.10/24
    command: mon
    restart: 'no'
 
